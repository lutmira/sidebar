import React from 'react';
//Custom Components
import {CustomCard} from "../../../components";

const HeavySpacing = () => {
  return (
    <CustomCard
      title={'HeavySpacing'}
      active={true}
      onClick={()=>{}}
      children={<></>}
    />
  );
};

export default HeavySpacing;