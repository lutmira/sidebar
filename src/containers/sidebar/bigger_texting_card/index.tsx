import React from 'react';
//Custom Components
import {CustomCard} from "../../../components";

const BiggerTexting = () => {
  return (
    <CustomCard
      title={''}
      active={true}
      onClick={()=>{}}
      children={<></>}
    />
  );
};

export default BiggerTexting;